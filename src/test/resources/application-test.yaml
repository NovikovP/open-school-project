spring:
  application:
    name: t1-hw

  jpa:
    generate-ddl: true
    open-in-view: true
  datasource:
    url: jdbc:postgresql://localhost:5435/task_db
    username: postgres
    password: postgres